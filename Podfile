# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodBag' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodBag
    pod 'Alamofire', '~> 5.5'

  target 'FoodBagTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FoodBagUITests' do
    # Pods for testing
  end

end
